# 오프라인 앱 구조

App → 로컬 기록 복원 → Onboarding 또는 5개 탭 → Flash / Session.

서버 DB와 인증이 없습니다. AsyncStorage의 vocaquest:v1:guest 키를 유지합니다. Snapshot에는 설정, 학습 이벤트, 사용자 단어, 즐겨찾기와 개인 단어 ID가 저장됩니다. 쓰기 요청을 직렬화합니다. 기존 내부 account/profileDirty/merge 필드는 이전 형식 호환용이며 서버 연결은 없습니다.

진행도, XP, 일일 미션, 통계는 이벤트에서 계산합니다. 복습 기한 → 취약 → 목표 난이도 신규 순으로 선정합니다. 실패 10분, 성공 1/3/7/14/30일 간격에 응답시간과 체감난이도를 반영합니다.

108개 한영 단어와 예문은 앱에 포함됩니다. 네이티브 TTS/STT/알림을 사용합니다. STT는 텍스트 유사도이며 음소 채점이 아닙니다. APK 직접 배포는 사용자 인증이 아니므로 재전달을 차단하지 않습니다.
